@model List<ChatSite.Models.Product>

@{
    ViewData["Title"] = "Live NASDAQ Stocks";
}

<h2>@ViewData["Title"]</h2>

<form method="get" asp-action="FilterAll">
    <div style="margin-bottom:12px;">
        <label>Min Price: <input type="number" step="0.01" name="minPrice" /></label>
        <label>Max Price: <input type="number" step="0.01" name="maxPrice" /></label>
        <label>Sector:
            <select name="sector" multiple>
                @foreach(var s in Model.Select(p => p.Sector).Distinct())
                {
                    <option value="@s">@s</option>
                }
            </select>
        </label>
        <button type="submit">Filter</button>
    </div>
</form>

<table border="1" cellpadding="6" cellspacing="0" style="border-collapse:collapse; width:100%;">
    <thead>
        <tr>
            <th>Name</th>
            <th>Price</th>
            <th>Sector</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var p in Model)
        {
            <tr>
                <td>@p.Name</td>
                <td>@p.Price</td>
                <td>@p.Sector</td>
            </tr>
        }
    </tbody>
</table>
